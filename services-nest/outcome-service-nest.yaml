# GENERATED by tools/generate-nest-manifests.js — do not edit by hand.
# NestJS + TypeORM runtime for outcome-service. Coexists with the legacy split runtime (services/outcome-service.yaml)
# on port 4317; cut over by repointing OUTCOME_SERVICE_URL to this Service (see service-routing-configmap.nest.yaml).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: outcome-service-nest
  namespace: etraffica
  labels:
    app: outcome-service-nest
    variant: nest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: outcome-service-nest
  template:
    metadata:
      labels:
        app: outcome-service-nest
        variant: nest
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: outcome-service-nest
          # Same per-service image (carries the whole repo + typeorm/nest deps); command overridden
          # to run the nest start script instead of the baked legacy SERVICE_START_SCRIPT.
          image: 0680/etraffica.v2.outcome-service:latest
          imagePullPolicy: Always
          command: ["npm", "run", "start:outcome-service-nest"]
          ports:
            - containerPort: 4317
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          env:
            - name: PORT
              value: "4317"
            # Foreign reads during the strangler window go through the monolith's read-model bridge.
            - name: READ_MODEL_SERVICE_URL
              value: "http://api-gateway:4300"
            - name: IDENTITY_SERVICE_URL
              value: "http://api-gateway:4300"
            # Domain-event producer (emitDomainEvent) — fail-soft when absent (no emit, write dropped).
            - name: AMQP_URL
              valueFrom:
                secretKeyRef:
                  name: etraffica-secrets
                  key: RABBITMQ_URL
                  optional: true
            - name: INTERNAL_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: etraffica-secrets
                  key: INTERNAL_SERVICE_TOKEN
                  optional: true
          # TCP probes (the nest bootstrap does not guarantee a health route on every service).
          livenessProbe:
            tcpSocket:
              port: 4317
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 4317
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "640Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: outcome-service-nest
  namespace: etraffica
  labels:
    app: outcome-service-nest
spec:
  type: ClusterIP
  selector:
    app: outcome-service-nest
  ports:
    - name: http
      port: 4317
      targetPort: 4317
