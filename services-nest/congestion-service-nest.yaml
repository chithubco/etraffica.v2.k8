# GENERATED by tools/generate-nest-manifests.js — do not edit by hand.
# NestJS + TypeORM runtime for congestion-service — the sole runtime for this service (the legacy split
# runtime was deleted). The gateway proxies its routes here via CONGESTION_SERVICE_URL on port 4315
# (see service-routing-configmap.yaml).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: congestion-service-nest
  namespace: etraffica
  labels:
    app: congestion-service-nest
    variant: nest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: congestion-service-nest
  template:
    metadata:
      labels:
        app: congestion-service-nest
        variant: nest
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: congestion-service-nest
          # Same per-service image (carries the whole repo + typeorm/nest deps); command runs the
          # nest start script (also the image's baked default CMD — explicit here for clarity).
          image: 0680/etraffica.v2.congestion-service:latest
          imagePullPolicy: Always
          command: ["npm", "run", "start:congestion-service-nest"]
          ports:
            - containerPort: 4315
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          env:
            - name: PORT
              value: "4315"
            # Foreign reads go through the gateway's read-model bridge.
            - name: READ_MODEL_SERVICE_URL
              value: "http://api-gateway:4300"
            - name: IDENTITY_SERVICE_URL
              value: "http://api-gateway:4300"
            # payments-media forwards processed provider callbacks to operations (the
            # certification cross-service join); harmless for the other services.
            - name: OPERATIONS_SERVICE_URL
              value: "http://operations-service-nest:4321"
            # Domain-event producer (emitDomainEvent) — fail-soft when absent (no emit, write dropped).
            - name: AMQP_URL
              valueFrom:
                secretKeyRef:
                  name: etraffica-secrets
                  key: RABBITMQ_URL
                  optional: true
            - name: INTERNAL_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: etraffica-secrets
                  key: INTERNAL_SERVICE_TOKEN
                  optional: true
          # TCP probes (the nest bootstrap does not guarantee a health route on every service).
          livenessProbe:
            tcpSocket:
              port: 4315
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 4315
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "640Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: congestion-service-nest
  namespace: etraffica
  labels:
    app: congestion-service-nest
spec:
  type: ClusterIP
  selector:
    app: congestion-service-nest
  ports:
    - name: http
      port: 4315
      targetPort: 4315
