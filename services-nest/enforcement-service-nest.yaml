# GENERATED by tools/generate-nest-manifests.js — do not edit by hand.
# NestJS + TypeORM runtime for enforcement-service. Coexists with the legacy split runtime (services/enforcement-service.yaml)
# on port 4319; cut over by repointing ENFORCEMENT_SERVICE_URL to this Service (see service-routing-configmap.nest.yaml).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: enforcement-service-nest
  namespace: etraffica
  labels:
    app: enforcement-service-nest
    variant: nest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: enforcement-service-nest
  template:
    metadata:
      labels:
        app: enforcement-service-nest
        variant: nest
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: enforcement-service-nest
          # Same per-service image (carries the whole repo + typeorm/nest deps); command overridden
          # to run the nest start script instead of the baked legacy SERVICE_START_SCRIPT.
          image: 0680/etraffica.v2.enforcement-service:latest
          imagePullPolicy: Always
          command: ["npm", "run", "start:enforcement-service-nest"]
          ports:
            - containerPort: 4319
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          env:
            - name: PORT
              value: "4319"
            # Foreign reads during the strangler window go through the monolith's read-model bridge.
            - name: READ_MODEL_SERVICE_URL
              value: "http://api-gateway:4300"
            - name: IDENTITY_SERVICE_URL
              value: "http://api-gateway:4300"
            # Domain-event producer (emitDomainEvent) — fail-soft when absent (no emit, write dropped).
            - name: AMQP_URL
              valueFrom:
                secretKeyRef:
                  name: etraffica-secrets
                  key: RABBITMQ_URL
                  optional: true
            - name: INTERNAL_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: etraffica-secrets
                  key: INTERNAL_SERVICE_TOKEN
                  optional: true
          # TCP probes (the nest bootstrap does not guarantee a health route on every service).
          livenessProbe:
            tcpSocket:
              port: 4319
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 4319
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "640Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: enforcement-service-nest
  namespace: etraffica
  labels:
    app: enforcement-service-nest
spec:
  type: ClusterIP
  selector:
    app: enforcement-service-nest
  ports:
    - name: http
      port: 4319
      targetPort: 4319
