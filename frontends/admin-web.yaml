# Static portal served by nginx (etraffica/static-portal.Dockerfile, listens on 80).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-web
  namespace: etraffica
  labels:
    app: admin-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-web
  template:
    metadata:
      labels:
        app: admin-web
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: admin-web
          image: 0680/etraffica.admin-web:26.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "10m"
              memory: "32Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: admin-web
  namespace: etraffica
  labels:
    app: admin-web
spec:
  type: ClusterIP
  selector:
    app: admin-web
  ports:
    - name: http
      port: 80
      targetPort: 80
