# Static portal served by nginx (etraffica/static-portal.Dockerfile, listens on 80).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: violator-web
  namespace: etraffica
  labels:
    app: violator-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: violator-web
  template:
    metadata:
      labels:
        app: violator-web
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: violator-web
          image: 0680/etraffica.violator-web:29.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "10m"
              memory: "32Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: violator-web
  namespace: etraffica
  labels:
    app: violator-web
spec:
  type: ClusterIP
  selector:
    app: violator-web
  ports:
    - name: http
      port: 80
      targetPort: 80
