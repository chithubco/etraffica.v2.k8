apiVersion: apps/v1
kind: Deployment
metadata:
  name: vehicle-warning-service
  namespace: etraffica
  labels:
    app: vehicle-warning-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vehicle-warning-service
  template:
    metadata:
      labels:
        app: vehicle-warning-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: vehicle-warning-service
          image: 0680/etraffica.v2.vehicle-warning-service:30.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4308
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4308
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4308
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: vehicle-warning-service
  namespace: etraffica
  labels:
    app: vehicle-warning-service
spec:
  type: ClusterIP
  selector:
    app: vehicle-warning-service
  ports:
    - name: http
      port: 4308
      targetPort: 4308
