apiVersion: apps/v1
kind: Deployment
metadata:
  name: vehicle-service
  namespace: etraffica
  labels:
    app: vehicle-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vehicle-service
  template:
    metadata:
      labels:
        app: vehicle-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: vehicle-service
          image: 0680/etraffica.v2.vehicle-service:26.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4303
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4303
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4303
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: vehicle-service
  namespace: etraffica
  labels:
    app: vehicle-service
spec:
  type: ClusterIP
  selector:
    app: vehicle-service
  ports:
    - name: http
      port: 4303
      targetPort: 4303
