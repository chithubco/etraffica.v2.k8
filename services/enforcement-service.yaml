apiVersion: apps/v1
kind: Deployment
metadata:
  name: enforcement-service
  namespace: etraffica
  labels:
    app: enforcement-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: enforcement-service
  template:
    metadata:
      labels:
        app: enforcement-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: enforcement-service
          image: 0680/etraffica.v2.enforcement-service:26.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4319
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4319
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4319
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: enforcement-service
  namespace: etraffica
  labels:
    app: enforcement-service
spec:
  type: ClusterIP
  selector:
    app: enforcement-service
  ports:
    - name: http
      port: 4319
      targetPort: 4319
