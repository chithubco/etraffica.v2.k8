apiVersion: apps/v1
kind: Deployment
metadata:
  name: learning-service
  namespace: etraffica
  labels:
    app: learning-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: learning-service
  template:
    metadata:
      labels:
        app: learning-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: learning-service
          image: 0680/etraffica.v2.learning-service:30.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4325
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4325
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4325
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: learning-service
  namespace: etraffica
  labels:
    app: learning-service
spec:
  type: ClusterIP
  selector:
    app: learning-service
  ports:
    - name: http
      port: 4325
      targetPort: 4325
