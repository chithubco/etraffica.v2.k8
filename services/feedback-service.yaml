apiVersion: apps/v1
kind: Deployment
metadata:
  name: feedback-service
  namespace: etraffica
  labels:
    app: feedback-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: feedback-service
  template:
    metadata:
      labels:
        app: feedback-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: feedback-service
          image: 0680/etraffica.v2.feedback-service:30.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4324
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4324
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4324
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: feedback-service
  namespace: etraffica
  labels:
    app: feedback-service
spec:
  type: ClusterIP
  selector:
    app: feedback-service
  ports:
    - name: http
      port: 4324
      targetPort: 4324
