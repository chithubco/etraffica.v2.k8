apiVersion: apps/v1
kind: Deployment
metadata:
  name: watchlist-service
  namespace: etraffica
  labels:
    app: watchlist-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: watchlist-service
  template:
    metadata:
      labels:
        app: watchlist-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: watchlist-service
          image: 0680/etraffica.v2.watchlist-service:26.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4310
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4310
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4310
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: watchlist-service
  namespace: etraffica
  labels:
    app: watchlist-service
spec:
  type: ClusterIP
  selector:
    app: watchlist-service
  ports:
    - name: http
      port: 4310
      targetPort: 4310
