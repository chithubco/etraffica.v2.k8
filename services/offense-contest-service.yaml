apiVersion: apps/v1
kind: Deployment
metadata:
  name: offense-contest-service
  namespace: etraffica
  labels:
    app: offense-contest-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: offense-contest-service
  template:
    metadata:
      labels:
        app: offense-contest-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: offense-contest-service
          image: 0680/etraffica.v2.offense-contest-service:26.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4309
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4309
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4309
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: offense-contest-service
  namespace: etraffica
  labels:
    app: offense-contest-service
spec:
  type: ClusterIP
  selector:
    app: offense-contest-service
  ports:
    - name: http
      port: 4309
      targetPort: 4309
