apiVersion: apps/v1
kind: Deployment
metadata:
  name: congestion-service
  namespace: etraffica
  labels:
    app: congestion-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: congestion-service
  template:
    metadata:
      labels:
        app: congestion-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: congestion-service
          image: 0680/etraffica.v2.congestion-service:30.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4315
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4315
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4315
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: congestion-service
  namespace: etraffica
  labels:
    app: congestion-service
spec:
  type: ClusterIP
  selector:
    app: congestion-service
  ports:
    - name: http
      port: 4315
      targetPort: 4315
