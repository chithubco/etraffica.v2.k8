apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-service
  namespace: etraffica
  labels:
    app: workflow-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: workflow-service
  template:
    metadata:
      labels:
        app: workflow-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: workflow-service
          image: 0680/etraffica.v2.workflow-service:26.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4307
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4307
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4307
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-service
  namespace: etraffica
  labels:
    app: workflow-service
spec:
  type: ClusterIP
  selector:
    app: workflow-service
  ports:
    - name: http
      port: 4307
      targetPort: 4307
