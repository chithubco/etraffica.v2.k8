apiVersion: apps/v1
kind: Deployment
metadata:
  name: operations-service
  namespace: etraffica
  labels:
    app: operations-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: operations-service
  template:
    metadata:
      labels:
        app: operations-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: operations-service
          image: 0680/etraffica.v2.operations-service:26.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4321
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4321
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4321
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: operations-service
  namespace: etraffica
  labels:
    app: operations-service
spec:
  type: ClusterIP
  selector:
    app: operations-service
  ports:
    - name: http
      port: 4321
      targetPort: 4321
