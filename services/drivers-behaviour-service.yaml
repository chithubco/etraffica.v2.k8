apiVersion: apps/v1
kind: Deployment
metadata:
  name: drivers-behaviour-service
  namespace: etraffica
  labels:
    app: drivers-behaviour-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: drivers-behaviour-service
  template:
    metadata:
      labels:
        app: drivers-behaviour-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: drivers-behaviour-service
          image: 0680/etraffica.v2.drivers-behaviour-service:30.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4314
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4314
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4314
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: drivers-behaviour-service
  namespace: etraffica
  labels:
    app: drivers-behaviour-service
spec:
  type: ClusterIP
  selector:
    app: drivers-behaviour-service
  ports:
    - name: http
      port: 4314
      targetPort: 4314
