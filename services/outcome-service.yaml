apiVersion: apps/v1
kind: Deployment
metadata:
  name: outcome-service
  namespace: etraffica
  labels:
    app: outcome-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: outcome-service
  template:
    metadata:
      labels:
        app: outcome-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: outcome-service
          image: 0680/etraffica.v2.outcome-service:30.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4317
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4317
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4317
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: outcome-service
  namespace: etraffica
  labels:
    app: outcome-service
spec:
  type: ClusterIP
  selector:
    app: outcome-service
  ports:
    - name: http
      port: 4317
      targetPort: 4317
