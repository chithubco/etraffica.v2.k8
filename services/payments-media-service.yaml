apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-media-service
  namespace: etraffica
  labels:
    app: payments-media-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payments-media-service
  template:
    metadata:
      labels:
        app: payments-media-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: payments-media-service
          image: 0680/etraffica.v2.payments-media-service:30.06.26.01
          imagePullPolicy: Always
          ports:
            - containerPort: 4320
              name: http
          envFrom:
            - configMapRef:
                name: etraffica-config
            - secretRef:
                name: etraffica-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4320
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4320
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: payments-media-service
  namespace: etraffica
  labels:
    app: payments-media-service
spec:
  type: ClusterIP
  selector:
    app: payments-media-service
  ports:
    - name: http
      port: 4320
      targetPort: 4320
